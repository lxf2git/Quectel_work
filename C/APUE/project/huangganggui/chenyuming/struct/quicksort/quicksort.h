#ifndef _QUICKSORT_H_
#define _QUICKSORT_H_

#include "pub.h"

void quicksort(struct list *);
struct list *sort_swap(struct list *, struct list *);
void sort_fun(struct list *, struct list *, struct list *);


#endif
